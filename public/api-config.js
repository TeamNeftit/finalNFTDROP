// Backend API URL - Railway deployment with custom domain
// This is safe to be public - it's just the API endpoint URL
// Secrets (API keys, tokens) are stored securely in Railway environment variables
window.API_BACKEND_URL = 'https://api.neftit.site';
